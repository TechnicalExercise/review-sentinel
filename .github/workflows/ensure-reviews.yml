# This is a basic workflow to help you get started with Actions

name: Ensure review policy in new repos

# Controls when the workflow will run
on:
  # Triggers the workflow on push or pull request events but only for the main branch
  repository_dispatch:
    types:
      - repo_created

  # Allows you to run this workflow manually from the Actions tab
  workflow_dispatch:
    inputs:
      repo-name:
        description: 'Name of the newly created repository'
        required: true
        default: 'repo1'

# A workflow run is made up of one or more jobs that can run sequentially or in parallel
jobs:
  # This workflow contains a single job called "build"
  ensure-review:
    name: 'Ensure review process for ${{ github.event.inputs.repo-name }}'
    # The type of runner that the job will run on
    runs-on: ubuntu-latest
    
    # Steps represent a sequence of tasks that will be executed as part of the job
    steps:
      - name: Get Token
        id: get_workflow_token
        uses: peter-murray/workflow-application-token-action@v1
        with:
          application_id: ${{ secrets.APPLICATION_ID }}
          application_private_key: ${{ secrets.APPLICATION_PRIVATE_KEY }}
      
      - name: Get repo
        id: get-repo
        uses: octokit/request-action@v2.x
        env:
          GITHUB_TOKEN: ${{ steps.get_workflow_token.outputs.token }} # ${{ secrets.GITHUB_TOKEN }}
          OWNER: ${{ github.repository_owner }}
          REPO: ${{ github.event.inputs.repo-name }}
        with:
          route: GET /repos/{owner}/{repo}
          owner: ${{ env.OWNER }}
          repo: ${{ env.REPO }} 
      
      - name: Show the repo data # should be default_branch
        run: |
          echo 'Default branch: ${{ fromJson(steps.get-repo.outputs.data).default_branch }}'

      - name: Get default branch protection from template repo
        id: get-branch-protection-template
        uses: octokit/request-action@v2.x
        env:
          GITHUB_TOKEN: ${{ steps.get_workflow_token.outputs.token }} # ${{ secrets.GITHUB_TOKEN }}
          OWNER: ${{ github.repository_owner }}
          REPO: template
          BRANCH: main
        with:
          route: GET /repos/{owner}/{repo}/branches/{branch}/protection
          owner: ${{ env.OWNER }}
          repo: ${{ env.REPO }} 
          branch: ${{ env.BRANCH }}
      
      - name: Show template repo branch protection rules
        id: template-repo-rules
        run: echo "${{ fromJson(steps.get-branch-protection-template.outputs).data }}"
          
#       - name: Set branch protection
#         id: set-protection
#         uses: octokit/request-action@v2.x
#         env:
#           GITHUB_TOKEN: ${{ steps.get_workflow_token.outputs.token }} # ${{ secrets.GITHUB_TOKEN }}
#           OWNER: ${{ github.repository_owner }}
#           REPO: ${{ github.event.inputs.repo-name }}
#           BRANCH: ${{ fromJson(steps.get-repo.outputs.data).default_branch }}
#         with:
#           route: PUT /repos/{owner}/{repo}/branches/{branch}/protection
#           owner: ${{ env.OWNER }}
#           repo: ${{ env.REPO }}       
#           branch: ${{ env.BRANCH }}
#           restrictions: ${{ fromJson(null) }}
#           required_status_checks: ${{ fromJson(null) }}
#           enforce_admins: true
#           required_pull_request_reviews: '${{ fromJson("{dismiss_stale_review: true}") }}'
          
